# Baked in at build time via Maven resource filtering (see root pom.xml's `free` profile and
# warden-core/pom.xml's filtered-resource config). Never read from config.yml - see
# docs/spec/08-TIERS-AND-LICENSING.txt SS3 for why the free/paid split must not be a user-editable
# runtime switch.
edition=${warden.edition}
