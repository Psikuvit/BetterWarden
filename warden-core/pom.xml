<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>me.psikuvit</groupId>
        <artifactId>betterwarden-parent</artifactId>
        <version>1.0</version>
    </parent>

    <artifactId>warden-core</artifactId>
    <packaging>jar</packaging>

    <name>warden-core</name>
    <description>Platform-free Spring Boot core: entities, repos, services. No Bukkit/Velocity imports - see PlatformBridge.</description>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-websocket</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-flyway</artifactId>
        </dependency>
        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
            <version>3.53.4.0</version>
        </dependency>
        <dependency>
            <!-- version managed by the spring-boot-dependencies BOM -->
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
        </dependency>
        <dependency>
            <!-- pinned to 12.4.0 to match the flyway-core version the Boot BOM manages; 13.x throws AbstractMethodError against it -->
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-nc-sqlite</artifactId>
            <version>12.4.0</version>
        </dependency>
        <dependency>
            <!-- flyway-core alone doesn't know MySQL's dialect - found this the hard way by
                 actually booting against a real MySQL container: "Unsupported Database: MySQL 8.4".
                 Same version-pinning reasoning as flyway-database-nc-sqlite above. -->
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-mysql</artifactId>
            <version>12.4.0</version>
        </dependency>
        <dependency>
            <groupId>org.hibernate.orm</groupId>
            <artifactId>hibernate-community-dialects</artifactId>
            <version>7.4.0.Final</version>
        </dependency>
        <dependency>
            <!-- docs/spec/05-DISCORD-BOT.txt: "JDA, running inside Core" - so every platform
                 (embedded or standalone) gets the bot for free, not a per-platform reimplementation.
                 slf4j-api version matches what's already resolved here (2.0.18), confirmed via
                 JDA's own POM before adding this - no version conflict to relocate around. -->
            <groupId>net.dv8tion</groupId>
            <artifactId>JDA</artifactId>
            <version>6.5.0</version>
        </dependency>
    </dependencies>
</project>
