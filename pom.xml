<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>me.psikuvit</groupId>
    <artifactId>betterwarden-parent</artifactId>
    <version>1.0</version>
    <packaging>pom</packaging>

    <name>BetterWarden</name>

    <modules>
        <module>warden-core</module>
        <!-- warden-spigot merged into warden-bukkit (me.psikuvit.betterWarden.spigot) - see
             warden-bukkit/pom.xml. One jar now ships both plugin.yml and paper-plugin.yml; the
             server's own loader picks the right one. Renamed from warden-paper since it now
             covers both Paper and plain Spigot, not just Paper. -->
        <module>warden-bukkit</module>
        <module>warden-velocity</module>
        <module>warden-bungee</module>
        <module>warden-standalone</module>
    </modules>

    <properties>
        <!-- paper-api needs JDK 25 to compile against; output bytecode stays at 21 (shade plugin's ASM can't rewrite class file 69 yet). -->
        <java.version>25</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <spring-boot.version>4.1.0</spring-boot.version>
        <maven.compiler.release>21</maven.compiler.release>
        <!-- docs/spec/08-TIERS-AND-LICENSING.txt - baked into warden-core's edition.properties via
             resource filtering. Build-time only, never read from config.yml (a free user editing
             config.yml must not be able to unlock paid features). -->
        <warden.edition>paid</warden.edition>
        <!-- Appended to warden-bukkit's two jar finalNames (BetterWarden.jar, BetterWarden-Spigot.jar)
             so the two editions are never confusable on disk (BetterWarden.jar vs
             BetterWarden-Free.jar) - see the `free` profile below. -->
        <warden.jarSuffix></warden.jarSuffix>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>me.psikuvit</groupId>
                <artifactId>warden-core</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <defaultGoal>clean package</defaultGoal>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <release>${maven.compiler.release}</release>
                        <parameters>true</parameters>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <repositories>
        <repository>
            <id>papermc-repo</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
        <repository>
            <id>placeholderapi</id>
            <url>https://repo.helpch.at/releases/</url>
        </repository>
    </repositories>

    <profiles>
        <!-- mvn package -Pfree - warden-bukkit only, see docs/spec/08-TIERS-AND-LICENSING.txt.
             No profile (default) keeps today's behaviour unchanged: full paid build. -->
        <profile>
            <id>free</id>
            <properties>
                <warden.edition>free</warden.edition>
                <warden.jarSuffix>-Free</warden.jarSuffix>
            </properties>
        </profile>
    </profiles>
</project>
