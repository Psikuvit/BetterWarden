package me.psikuvit.betterWarden.core.model;

/** Only GAME is actually produced right now - DISCORD/WEB exist for when the bot/panel land. */
public enum MessageSource {
    GAME,
    DISCORD,
    WEB
}
